<%= form_with model: @comment, url: product_comments_path do |form| %>
  <%= form.hidden_field :product_id, value: @comment.product_id %>

  <div>
    <%= form.rich_text_area :body %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
